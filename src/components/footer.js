import React, { Component } from 'react';
import styles from '../styles/main';

class Footer extends Component {
  render() {
    return <footer style={styles.footer}>
      Contact us at fax-o@tron.com
    </footer>
  }
}

export default Footer;
